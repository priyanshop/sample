import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
   alertContainer: {
      flexDirection: 'row',
      alignItems: 'center',
      padding: 15,
      paddingVertical: 20,
      borderRadius: 10,
      gap: 10,
   },
   iconWrapper: {
      marginTop: 2,
      alignSelf: 'flex-start',
   },
   textWrapper: {
      flex: 1,
   },
});
